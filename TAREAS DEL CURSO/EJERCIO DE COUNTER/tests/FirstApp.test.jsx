/* 


import {getByTestId, render} from "@testing-library/react";
import { FirstApp } from "../src/components/FirstApp";
// eslint-disable-next-line no-undef
describe("FirstApp", () => {
/* test del snapshot
test("prueba uno hacer macth  snapshot", () => {
 const {container}=render(<FirstApp />);
 expect(container).toMatchSnapshot();
})
test("prueba de mostar nombre con un data-id", () => {
    const nombre = "carlos";
    const {container,getByText,getByTestId}=render(<FirstApp nombre={nombre} />);
    expect(getByText(nombre)).toBeTruthy();
    expect(getByTestId("test-nombre").innerHTML).toBe(nombre);
   })
   

})*/