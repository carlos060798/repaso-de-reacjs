/*
import { getUser, getUsuarioActivo } from "../src/pruebasJs/funciones";
describe ('Pruebas en el archivo funciones.js', () => {


 test('debe de retornar un objeto', () => {
  
    const userTest = {
        uid: 'ABC567',
        username: 'El_Papi1502'
    } 
    const user = getUser(); 

    expect(userTest).toEqual(user); 

 });
test('debe de retornar un objeto con el nombre', () => {

    const nombre= 'carlos';
    const user= getUsuarioActivo(nombre);

    expect(user).toEqual({uid: 'ABC567', username: nombre})
});

}) 

*/