import { createContext } from "react";  

// createContext() es una funcion que crea un contexto que envia datos a los componentes hijos sin nesesidad de pasarlos por props

export const userContext= createContext();